# TODO

進め方は `docs/app-design-plan.md`、画面の骨格は `docs/wireframes.md`、
テーマは `docs/theme.md` を参照。

## 段階 2: Claude Design でコンポーネントを作る

- [x] テーマ（色・タイポグラフィ）
- [x] シルエット表示・コーディネート画面
- [x] アイテム一覧・保存コーデ一覧
- [x] ゴーストの濃さを決める → いちばん薄い案（`docs/theme.md` の `ghost-fill` /
      `ghost-stroke`）
- [x] 掴んでいる間の「なし」の見せ方を決める → 白い透過の面（`oklch(1 0 0 / 30%)`）＋
      青墨の実線（`oklch(0.26 0.018 290 / 70%)` / `stroke-width: 0.75`）。輪郭は外周
      1 本の path で引く（羽織は `haori-shape`、ゴーストの半襟は `haneri-shape`。追加済み）
- [x] ダーク（夜桜）での値を決める → なしはライトと同じ値、ゴーストは地に追従して反転。
      塗りは現行の色の 70% 透過（`docs/theme.md` の「空の表現の色」）
    - [x] ダークのシルエット内の線が黒になる問題と、その線から着物がちらっと見える問題
          → 外周 1 本の path を本体の下に敷いて隙間を埋め、色は地に追従しない `seam`
          （`oklch(0.99 0.007 350)`）で固定する
- [x] 決めた値をプレビューとドキュメントに反映する
    - [ ] テーマ見本 `design/templates/theme/colors.html` が `docs/theme.md` と
          食い違っているので合わせる。`ghost-stroke` が `0.78 0.02 330`（正は
          `0.62 0.02 330`）、`ghost-fill` に透過が無く、`none-fill` / `none-stroke` /
          `seam` は見本に載っていない
- [ ] デザインシステムとアプリ具体のデザインが混ざっているので整理する

## 段階 3: 実装

- [ ] テーマを適用する
    - [ ] `@fontsource/kaisei-decol` と `@fontsource/zen-maru-gothic` を導入する
    - [ ] `src/main.css` のトークンを `docs/theme.md` の値に差し替える
- [ ] コーディネート画面を作り直す
    - [ ] インジケーター列と選択情報テキストを外す
    - [ ] 保存ボタンとメニューを足す
    - [ ] 着物・羽織・半襟に**外周 1 本**の path を足す（`KimonoSilhouette` /
          `HaoriSilhouette` / `HaneriSilhouette`）。内部の線（衿の線・裾上の線・
          袖の切れ目・右前の隙間・左右の半襟の境界）は細長い塗りや 5 単位の隙間で
          表されており、**線そのものを描いていない**ため、これが 2 つの役割を持つ
        - 本体の**下**に敷いて隙間を埋める下敷き。塗りは `seam` で、地に追従させない
        - ゴーストや「なし」のときの輪郭。本体に枠線を引くと 1 本の線が 2 本の破線に
          なってしまうため、外周だけを通って隙間を跨ぐ
        - `design/templates/partials/silhouette.html` の `kimono-shape` / `haori-shape` /
          `haneri-shape` をそのまま使える
    - [ ] 未登録のゴースト（タップで追加）と「なし」を実装する
    - [ ] なしの領域に触れている間は面と実線を出す（値は段階 2 で確定。ゴーストは
          薄い桜色の面＋破線、なしは白い面＋青墨の実線で区別する）
    - [ ] 起動時に前回の組み合わせを復元する
- [ ] アイテム一覧 `/items` を作る
    - [ ] カテゴリチップ・1 列リスト・候補トグル
    - [ ] 追加 FAB（写真 / 色）と、名前のインライン編集・スワイプ削除・並べ替え
- [ ] 保存コーデ一覧 `/coordinates` を作る
- [ ] ログイン画面を作る
- [ ] Supabase につなぐ
    - [x] アカウント作成
    - [ ] **テーブルと Storage の RLS を先に設定する**（あとから足すと公開期間ができる）
    - [ ] アイテムとコーディネートの読み書き
- [ ] 背景透過を組み込む（`@imgly/background-removal`、処理中・失敗の行）
- [ ] vercel にあげてスマホで確認する

## まだ決めていないこと

- [ ] 画像をシルエットの形に沿って描画する方法（現状は帯留めのみ画像に対応）
- [ ] 帯留めと帯締めの当たり判定の重なり。帯留めがなしのとき、帯締めの中央を
      どちらに割り当てるか（`KimonoView` は現状ずっと帯留めを優先する）
- [ ] 帯揚げと帯の重なり。帯揚げ（`y=85.5〜92`）は下端の 2 単位が帯（`y=90〜160`）に
      潜り込んでおり、帯が登録済みなら隠れるが、初回は帯もゴーストなので**塗りの透過越しに
      その破線が見えてしまう**。案は 2 つ。
    - ゴースト用に下端を閉じない path（`o-ghost`）を足す。潜り込みの造形は残るが、
      レイヤーごとに 2 つの path を持つ構造になり、ゴースト用 path が 4 つに増える
    - 帯揚げ自体を帯と重ならない形にする。構造はきれいになるが、着付けの積み重なりが
      消え、当たり判定の高さも 6.5 → 4.5 単位に減って細い部位が掴みにくくなる

## その他

- [ ] React Compiler の準備
    - [x] useMemo や useCallback の禁止
    - [x] react-hooks の関連ルール導入
- [ ] 全体的にリファクタリングできる部分がないか検討する

## 決めたことの記録

### 背景透過は `@imgly/background-removal` を使う

- 方針: クライアントサイド推論。SaaS API はバックエンドと月額費用が要るので見送り
- 帯留め画像で検証: 外形はきれいに抜けるが、透かし(穴)部分に背景が残る。
  処理時間は devcontainer の WASM で 20〜30 秒
- ライセンスは AGPL-3.0 なので、非 OSS で商用化する場合は要商用ライセンス
- `transformers.js` + BiRefNet_lite も試したが、ブラウザの WASM では
  fp32 / fp16 とも std::bad_alloc(メモリ不足)で断念(モデルは MIT で商用可)
- 検証ページは `/bg-test`(検証用画像は `public/obidome-sample.jpg`)

### 各カテゴリに単色を事前登録して 0 点にできなくする案は採らない

- 事前登録した単色は「所有している 1 点」ではないので削除され、結局 0 点に戻る。
  防ぐには「最後の 1 点は削除できない」制約が要り、モードレス方針とも噛み合わない
- 代わりに 0 点の見せ方を決めた。ゴーストを出すのは「無いと困るのに無い」ときだけで、
  羽織・帯留めは 0 点でも「なし」として何も描かない（`docs/wireframes.md`）

### 掴んでいる間の「なし」は面と実線で示す

- 破線は面積のわりに主張が強い。伝えたいのは境界ではなく「この範囲を掴んでいる」
  という領域なので、面を持たせる
- 単色の面だけでは下地依存が消えない。白い面は濃い着物で効いて淡い着物では消え、
  暗い面はその逆になる。着物の色はユーザーが登録するのでどちらにも賭けられない
- 彩度を抜く案（`mix-blend-mode: saturation`）も試した。下地には依存しないが、
  着物からはみ出す羽織の袖など背景（無彩色）に掛かる部分では何も変わらず輪郭が
  途切れる。効き方も分かりにくい
- 白い透過の面と青墨の実線を組み合わせると、濃い着物では面が、淡い着物では線が
  効くので、どちらの下地でも成立する
- 面 22 / 30 / 38% と線 100 / 70 / 50% を、濃い着物・淡い着物・帯留めで見比べて
  面 30% ・線 70% にした。38% も候補で、掴んだ範囲はよく立つが、着物の色そのものが
  変わって見えて脱いだ姿の確認を邪魔する（ダークの値を決めるときに再検討する）
- 線の弱さは淡い下地で決まる（濃い下地では面が支える）。100% は本文と同じ重さで
  一時的な表示にしては強く、50% は濃い着物で面に飲まれて消えかける
- 輪郭は外周 1 本の path で引く。羽織は袖と身頃が 5 単位の隙間で分かれており、
  そのままだと脇に細い線が 2 本並び、面も袖と身頃に分断されて 2 つの部品を掴んで
  いるように見える（`haori-shape` は袖の下端で身頃へ渡ってひと続きにする）
- ゴーストの半襟も同じで、左右 2 枚をそれぞれ囲むと境界の破線が交差して読めない。
  外辺（着物の V と重なる側）と内辺（首側）だけを通る `haneri-shape` にした。奥の衿を
  描かない案はさらにすっきりするが、ゴーストと登録済みで形が変わるので採らない

### ダークでは、なしはそのまま・ゴーストは反転する

- なしの下地はユーザーが登録したアイテムの色で、地の明暗とは関係ない。面と線を
  反対向きに持たせてある以上、ライトと同じ値がそのまま成立する。線だけ反転させる案は、
  面と線が同じ向きになって淡い着物の上で輪郭が消えた。面ごと反転させる案（黒い面）は
  成立はするが、着物の色が暗く濁って脱いだ姿の確認を邪魔する
- ゴーストの下地は地なので、こちらは反転が要る。ライトの値をそのまま持ち込むと初回の姿が
  真っ白なシルエットになり、登録済みのアイテムにしか見えなくなる

### ゴーストの塗りは透過にする

- 下地は地とは限らない。帯揚げと帯締めは登録済みの帯の上に重なるため、地に合わせた単色に
  すると帯の上で穴（ダーク）や白い浮き（ライト）になり、登録済みと紛らわしくなる
- 現行の色を 70% の透過にすると、下地が何であれ地へ引き寄せられ「そこだけ沈む」関係が
  保たれる。ライトの「なし」で単色の面を採らなかったのと同じ理屈
- ダークだけ地より明るく持ち上げる案も試したが、初回で浮きすぎて登録済みに見えた
- 青墨を薄く重ねる案（ライト 5〜8%）は、同じ位置にゴーストが 4 枚重なる初回で色が累積し、
  帯のあたりだけ濃い矩形になる。地に近い色を使えば累積は出ない
- 率は 50 / 60 / 70% を比べて 70% にした。塗りが地へ寄るほど破線が埋もれるので、
  輪郭は地から約 0.38 離す（ライト 0.62 / ダーク 0.65）。ダークの現行 0.45 は帯の上で
  塗りとほぼ同じ明度になり、破線がほとんど消えていた

### シルエットの内部の線は地に追従させない

- 内部の線は線として描いておらず、塗りの隙間で表している。下に何も無ければ地が透けるので、
  ダークでは黒い切れ目になり、布の縫い目ではなく「穴」に見える
- 羽織の袖と身頃の切れ目も同じ隙間で、下が着物なので着物の色が覗く。地の明暗に関係なく
  起きるが、ダークでは線が黒いぶん赤が目立つ
- 外周 1 本の path を本体の下に敷けば、隙間が塗りで埋まって両方いっぺんに直る。
  形は既にある（`kimono-shape` / `haori-shape` / `haneri-shape`）ので、同じ path を
  下敷きとゴーストの輪郭で兼用する
- 色はライトの地（白桜 `oklch(0.99 0.007 350)`）に固定した。ライトの見え方は変わらず、
  ダークだけが直る。地に追従させる案は、ダークで黒くなるという問題そのものが戻る
- 着物や半襟が白系だと線が消えるが、ライトでは元からそうなっており、新たな劣化ではない

### スワイプは `react-swipeable` に移行しない

自前の `useSwipe` で足りており、移行の利点が無かった。

### `boundary` は使えなかった

導入を検討したが、この構成では動かせなかった。
